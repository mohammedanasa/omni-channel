services:
  postgres:
    image: postgres
    environment:
      POSTGRES_USER: postgres_multi_tenant_pos_admin
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: postgres_multi_tenant_pos
    ports:
      - "5434:5432"

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
